name: build

on:
  
  push:
    branches: 
      - 'succeed'
    tags:
      - 'v*'
  pull_request:
    branches: [ "succeed" ]

jobs:
  build:
    #if: ${{ !startwith(commit,#NORUN)}}
    runs-on: ubuntu-latest
    steps:
      # 2
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Trivy vulnerability scanner in repo mode
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          ignore-unfixed: true
          format: 'table'
          output: 'first.table'
          severity: 'CRITICAL'   
      

      # 4
      
     
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      -
        name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}
      
      -
        name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: yisong0319/cicd:${{github.sha}}
      
      -
        name: install cosign
        uses: sigstore/cosign-installer@v3.1.1
        with:
          cosign-release: 'v2.2.0'
      
      - name: Sign image with a key
        run: |
          cosign sign --yes --key env://COSIGN_PRIVATE_KEY "${TAGS}"
        env:
          TAGS: 'yisong0319/cicd:${{github.sha}}'
          COSIGN_PRIVATE_KEY: ${{ secrets.COSIGN_PRIVATE_KEY }}
          COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}

      # 
      
      - uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_CHANNEL: general
          SLACK_COLOR: 'good' # or a specific color like 'good' or '#ff00ff'
          SLACK_ICON: https://github.com/rtCamp.png?size=48
          SLACK_MESSAGE: |
            Name: ZOU YISONG
            Email: e1285206@u.nus.edu
            Git: <https://github.com/zys-319/cicd>
            Image: <https://hub.docker.com/repository/docker/yisong0319/cicd>
          SLACK_TITLE: Image build and signed
          SLACK_USERNAME: CICD
          SLACK_WEBHOOK: ${{secrets.WEBHOOK}}

      
      - uses: MeilCli/slack-upload-file@v3
        with:
          slack_token: ${{secrets.WEBOOKTESTTOKEN}}
          channel_id: ${{secrets.CHANNELID}}
          content: 'file content'
          file_type: 'txt'
          file_name: 'trivy-result.txt'
          title: 'title of file'
          initial_comment: 'post by ZOU YISONG'
